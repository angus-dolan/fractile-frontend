type Corpus = {
  id: number;
  org_id: number;
  name: string;
  created_at: Date;
  size: number;
  owner_id: number;
  description: string;
  files_cnt: number;
  c_type: string;
};

export type { Corpus };
